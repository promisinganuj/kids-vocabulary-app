# ─── VCE Vocabulary Flashcards — Environment Configuration ───────────────
#
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# Configuration priority (highest to lowest):
#   1. Environment variables
#   2. Docker secrets files (*_FILE env vars)
#   3. .env file values
#   4. Built-in defaults
#

# ─── Application ────────────────────────────────────────────────────────
# Environment: development | staging | production
# In production, SECRET_KEY and DATABASE_URL are REQUIRED (app won't start without them)
APP_ENV=development
# APP_NAME=VCE Vocabulary Flashcards
# DEBUG=false  (auto-set: true in dev, false in production)

# ─── Server ─────────────────────────────────────────────────────────────
# HOST=0.0.0.0
# PORT=5001
# WORKERS=1          # Increase for production (e.g., 4)
# RELOAD=false       # Auto-set to true in development

# ─── Security ───────────────────────────────────────────────────────────
# REQUIRED in production. Generate with: python -c "import secrets; print(secrets.token_hex(32))"
# SECRET_KEY=your-secret-key-here
# SESSION_TIMEOUT_HOURS=24

# For Docker/K8s secrets, point to a file instead:
# SECRET_KEY_FILE=/run/secrets/secret_key

# ─── Database ───────────────────────────────────────────────────────────
# REQUIRED in production. Default in dev: sqlite:///data/vocabulary.db
# For SQLite:  sqlite:///data/vocabulary.db
# For Postgres: postgresql://user:password@host:5432/dbname (future)
# DATABASE_URL=sqlite:///data/vocabulary.db
# DB_TIMEOUT=30

# For Docker/K8s secrets:
# DATABASE_URL_FILE=/run/secrets/database_url

# ─── Azure OpenAI (Optional) ───────────────────────────────────────────
# AI-powered word search and learning features. App works without these.
# AZURE_OPENAI_API_KEY=your-api-key
# AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
# AZURE_OPENAI_DEPLOYMENT=your-deployment-name
# AZURE_OPENAI_API_VERSION=2024-02-15-preview

# For Docker/K8s secrets:
# AZURE_OPENAI_API_KEY_FILE=/run/secrets/openai_api_key

# ─── Seed Data ──────────────────────────────────────────────────────────
# Path to initial vocabulary word list (relative to app/ directory)
# SEED_DATA_PATH=../seed-data/words-list.txt
